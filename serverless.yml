component: http
name: http-flask-q85w40KyYx
inputs:
  src:
    dist: ./
    exclude:
      - .env
    hook: pip install -r requirements.txt -t ./
  region: ap-guangzhou
  faas:
    framework: flask
    name: '${name}'
    runtime: Python3.6
    eip: false
    timeout: 3
    memorySize: 512
    tags: []
    environments: []
  apigw:
    protocols:
      - http
      - https
    timeout: 60
    environment: release
    customDomains: []
  isAutoCiDeploy: false
org: '1300061766'
app: feishu-bot
stage: dev
